 if ($(window).scrollTop() >= 1000 && $(window).scrollTop() <= 1200) {
        tl13.resume();
    }
    if ($(window).scrollTop() >= 3600 && $(window).scrollTop() <= 3800) {
        tl14.resume();
   }
