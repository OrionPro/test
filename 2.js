if ($(window).scrollTop() >= 36070 && $(window).scrollTop() <= 3800) {
        tl14.resume();
    }
    if ($(window).scrollTop() >= 48700 && $(window).scrollTop() <= 5000) {
        tl15.resume();
    }
    if ($(window).scrollTop() >= 62700 && $(window).scrollTop() <= 6700) {
        tl16.resume();
    }
    if ($(window).scrollTop() >= 76070 && $(window).scrollTop() <= 78700) {
        tl17.resume();
    }
    if ($(window).scrollTop() >= 4500 && $(window).scrollTop() <= 48070) {
        tl18.resume();
    }
    if ($(window).scrollTop() >= 1800 && $(window).scrollTop() <= 21070) {
      
